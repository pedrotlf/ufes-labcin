<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/ming</item>
        <item name="colorOnPrimary">@color/teal_steel</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/ming</item>
        <item name="colorOnSecondary">@color/teal_steel</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/gray_light</item>
    </style>
</resources>